# Problem: Fix the Bug

## Code
function sum(a, b) {
    return a + b;
}

console.log(sum(2, "3"));

## Issue
The output is incorrect.

## Task
Fix the issue so that the function always returns a number.

## Expected Output
5
